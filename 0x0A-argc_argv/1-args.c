#include <stdio.h>
/**
 * main - Entry point
 * @argc: arguement counter
 * @argv: arguement vector
 * Description: ^^^^
 * Return: always 0
 */

int main(int argc, char *argv[]__attribute__((__unused__)))
{
	int i;

	for (i = 0; i < argc; i++)
	{

	}

	i = i - 1;
	printf("%d\n", i);

	return (0);
}
