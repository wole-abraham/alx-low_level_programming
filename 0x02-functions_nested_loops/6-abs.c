#include "main.h"
/**
 * _abs - returns absolute value
 * Description: above
 * @c: arguement pased
 * Return: c
 */

int _abs(int c)
{
	if (c >= 0)
	{
		return (c);
	}

	else if (c < 0)
	{
		return (c * -1);
	}

}
