#include <stdio.h>

void rev_string(char *s)
{

	int i;
	int b;

	for(i = 0; *(s + i) != '\0'; i++)
	{
	}

	char str[i];

	for(; i >= 0; i--)
	{
		*(str + i) = *(s + i);
	}

	
	printf("%c", str[8]);

}


