#include <stdio.h>

/**
 * _strlen - entry point
 * @s: pointer of char
 * Description: oh
 * Return: i
 */
int main(void)
{

	char *s;
	int i;

	s = "ello world";
	printf("%c", s[1]);




	for (i = 0; s[i] != '\0'; i++)
	{

	}

	printf("%d", i);
	return (i);

}

